# لیست کامل دستورات ربات به زبان فارسی

در این فایل تمام دستورات موجود در ربات فایروال به همراه توضیحات کاربردی آنها به زبان فارسی آورده شده است. دستورات بر اساس عملکرد دسته‌بندی شده‌اند.

> **نکته:** تمام دستورات با پیشوند `!` یا `.` قابل استفاده هستند. (مثلاً `!ban` یا `.ban`)

---

## 🛡️ مدیریت و نظارت (Administrative & Moderation)

این دستورات برای مدیریت کاربران و نظم گروه استفاده می‌شوند.

| دستور | توضیحات | مثال |
| :--- | :--- | :--- |
| `!ban [time]` | بن کردن کاربر از گروه. اگر زمانی مشخص نشود، دائمی است. | `!ban 1h` (۱ ساعت) |
| `!kick` | اخراج کاربر از گروه (بدون بن شدن، کاربر می‌تواند برگردد). | `!kick` (روی ریپلای) |
| `!mute [time]` | سکوت (Mute) کردن کاربر. اگر زمانی مشخص نشود، دائمی است. | `!mute 30m` (۳۰ دقیقه) |
| `!unmute` | رفع سکوت کاربر و بازگرداندن دسترسی چت. | `!unmute` (روی ریپلای) |
| `!unban` | رفع بن کاربر (نیاز به آیدی کاربر دارد یا از طریق پنل). | `!unban` |
| `!warn` / `!warning` | تغییر وضعیت سیستم اخطار (روشن/خاموش). | `!warning on` |
| `!resetwarns` / `!reset` | پاک کردن تمام اخطارهای یک کاربر. | `!resetwarns` (روی ریپلای) |
| `!pin` | پین کردن پیام در گروه. | `!pin` (روی ریپلای) |
| `!unpin` | حذف پین تمام پیام‌ها. | `!unpin` |
| `!setphoto` | تنظیم عکس گروه (با ریپلای روی عکس). | `!setphoto` |
| `!setrules [text]` | تنظیم قوانین گروه. | `!setrules لطفا توهین نکنید` |
| `!rules` | نمایش قوانین گروه. | `!rules` |
| `!welcome [on/off]` | فعال/غیرفعال کردن پیام خوش‌آمدگویی. | `!welcome on` |
| `!joinleave [on/off]` | نمایش/عدم نمایش پیام‌های ورود و خروج توسط سرویس تلگرام. | `!joinleave off` |

---

## 🔒 قفل‌ها (Locks)

برای قفل کردن ارسال انواع محتوا در گروه.

**دستور کلی:** `!lock [نوع]` برای قفل کردن و `!unlock [نوع]` برای باز کردن.

**انواع قفل‌ها:**
- `link` (لینک‌ها)
- `url` (لینک‌های وب)
- `forward` (پیام‌های فرواردی)
- `sticker` (استیکر)
- `gif` (گیف)
- `video` (ویدیو)
- `msg` / `text` (متن)
- `photo` (عکس)
- `voice` (ویس)
- `audio` (آهنگ/فایل صوتی)
- `file` (فایل)
- `game` (بازی)
- `location` (موقعیت مکانی)
- `contact` (مخاطب)
- `poll` (نظرسنجی)
- `user` (منشن کردن کاربر)
- `bot` (اد کردن ربات)
- `inline` (دکمه‌های شیشه‌ای)
- `porn` (محتوای نامناسب - هوشمند)
- `slang` (فحاشی - هوشمند)
- `spam` (اسپم)

**سایر دستورات قفل:**
| دستور | توضیحات |
| :--- | :--- |
| `!lockgroup` | قفل کردن کامل گروه (کسی نمی‌تواند پیام دهد). |
| `!unlockgroup` | باز کردن قفل گروه. |
| `!silence[1-3] from [H:M] to [H:M]` | تنظیم زمان خاموشی خودکار گروه (مثلاً شب). |
| `!adminlock [on/off]` | قفل ادمین‌ها (آیا ادمین‌ها هم محدود شوند؟). |
| `!publiccmds [on/off]` | قفل دستورات عمومی (آیا کاربران عادی بتوانند دستور بزنند؟). |

---

## 🚫 فیلترینگ کلمات (Filtering)

مدیریت کلمات ممنوعه در گروه.

| دستور | توضیحات | مثال |
| :--- | :--- | :--- |
| `!filter [word]` | افزودن یک کلمه به لیست فیلتر (حذف پیام حاوی کلمه). | `!filter بی‌تربیت` |
| `!filterwarn [word]` | فیلتر کلمه + اخطار به کاربر. | `!filterwarn کلمه` |
| `!filterban [word]` | فیلتر کلمه + بن کردن کاربر. | `!filterban کلمه` |
| `!filtermute [word]` | فیلتر کلمه + سکوت کاربر. | `!filtermute کلمه` |
| `!unfilter [word]` | حذف کلمه از لیست فیلتر. | `!unfilter کلمه` |
| `!filterlist` | نمایش لیست کلمات فیلتر شده. | `!filterlist` |
| `!cleanfilters` | پاک کردن تمام کلمات فیلتر شده. | `!cleanfilters` |

---

## 👮 سطوح دسترسی (Ranks & Permissions)

مدیریت مدیران و کاربران ویژه.

| دستور | توضیحات |
| :--- | :--- |
| `!setowner` | تعیین کاربر به عنوان مالک (دسترسی کامل). |
| `!remowner` | حذف کاربر از مالکین. |
| `!ownerlist` | لیست مالکین. |
| `!promote` | ارتقاء کاربر به مدیر ربات (Manager). |
| `!demote` | عزل کاربر از مدیریت ربات. |
| `!modlist` | لیست مدیران ربات. |
| `!vip` / `!addvip` | افزودن کاربر به لیست VIP (ضد محدودیت). |
| `!remvip` | حذف کاربر از لیست VIP. |
| `!viplist` | لیست کاربران VIP. |
| `!whitelist [add/remove]` | مدیریت لیست سفید (کاربران مجاز در حالت قفل). |

---

## 🧹 پاکسازی (Cleanup)

دستورات برای پاکسازی پیام‌ها و داده‌های گروه.

| دستور | توضیحات | مثال |
| :--- | :--- | :--- |
| `!purge [count]` | حذف تعداد مشخصی از پیام‌های اخیر. | `!purge 100` |
| `!del` | حذف پیام ریپلای شده. | `!del` |
| `!setautoclean [count] [time]` | تنظیم پاکسازی خودکار پیام‌ها. | `!setautoclean 50 1h` |
| `!cleandeleted` | حذف اکانت‌های دلیت اکانت شده (Ghost) از گروه. | `!cleandeleted` |
| `!cleanbots` | حذف ربات‌های مخرب از گروه. | `!cleanbots` |
| `!cleannicknames` | حذف لقب‌های (Nickname) تنظیم شده کاربران. | `!cleannicknames` |
| `!cleanbans` | پاک کردن لیست بن‌ها (نیاز به دسترسی خاص). | `!cleanbans` |
| `!cleanwarns` | پاک کردن تمام اخطارهای همه کاربران. | `!cleanwarns` |

---

## 👥 عضویت اجباری (Force Join & Add)

وادار کردن کاربران به عضویت در کانال یا اد کردن مخاطب.

| دستور | توضیحات |
| :--- | :--- |
| `!forcejoin @channel` | تنظیم کانال برای جوین اجباری. |
| `!forcejoin off` | غیرفعال کردن جوین اجباری. |
| `!setforcejointext [text]` | تنظیم متن پیام جوین اجباری. |
| `!forceadd on` | فعال کردن قفل اد اجباری (کاربر باید اد بزند تا چت کند). |
| `!setforceadd [count]` | تنظیم تعداد اد مورد نیاز. |
| `!forceaddstatus [all/new]` | اعمال اد اجباری روی همه یا فقط اعضای جدید. |

---

## 🤖 آنتی تبچی (Anti-Tabchi)

محافظت در برابر ربات‌های تبلیغاتی (Tabchi).

| دستور | توضیحات |
| :--- | :--- |
| `!untabchi [id]` | حذف کاربر از لیست سیاه تبچی. |
| `!wltabchi` | افزودن کاربر به لیست سفید آنتی تبچی. |
| `!tabchiinfo` | استعلام وضعیت تبچی بودن یک کاربر. |

---

## 📊 آمار (Statistics)

| دستور | توضیحات |
| :--- | :--- |
| `!stats` | نمایش آمار کلی گروه (برترین دعوت‌کنندگان و...). |
| `!userstats` | نمایش آمار شخصی کاربر (تعداد پیام، دعوت و...). |
| `!daily / !weekly` | آمار روزانه یا هفتگی. |
| `!setautostats [time]` | تنظیم ارسال خودکار آمار در ساعت مشخص. |

---

## 🛠️ ابزارهای کاربردی (Utilities)

| دستور | توضیحات |
| :--- | :--- |
| `!id` / `!myid` | نمایش آیدی عددی کاربر و گروه. |
| `!whois` / `!info` | اطلاعات کامل کاربر (ریپلای). |
| `!weather [city]` | نمایش وضعیت آب و هوا. |
| `!translate [lang] [text]` | ترجمه متن (مثلاً `!tr fa Hello`). |
| `!currency` / `!rate` | نمایش نرخ ارزها. |
| `!time` | نمایش ساعت و تاریخ دقیق. |
| `!azan [city]` | نمایش اوقات شرعی شهر مورد نظر. |
| `!sticker` | تبدیل عکس به استیکر. |
| `!link` | دریافت لینکدعوت گروه. |

---

## 🎉 سرگرمی (Fun)

| دستور | توضیحات |
| :--- | :--- |
| `!joke` | ارسال جوک تصادفی (انگلیسی). |
| `!fortune` | فال روزانه / جملات انگیزشی. |
| `!poetry` | ارسال شعر/جمله زیبا. |
| `!font [text]` | نوشتن متن با فونت‌های زیبا. |
| `!news` | دریافت آخرین اخبار جهان. |

---

## ⚙️ دیگر تنظیمات

| دستور | توضیحات |
| :--- | :--- |
| `!settings` | نمایش پنل تنظیمات کلی ربات. |
| `!userpanel` | باز کردن پنل مدیریت کاربر خاص (ریپلای). |
| `!config` | بارگذاری مجدد تنظیمات (Reload). |
